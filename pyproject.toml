[project]
name = "LilyGo"
version = "0.1.0"
description = "CircuitPython code to run clock and temp sensor on LilyGo ESP32-S3"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "circup>=2.2.3",
    "requests>=2.32.5",
]
